# On-Premises-to-AWS-Cloud-Migration
A simulated Notes application deployed in an on-premises environment using a virtual machine (VM) and MySQL database. This project focuses on migrating the application and its database from the on-premises infrastructure to AWS.
